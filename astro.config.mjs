// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // This is your live URL
  site: 'https://svoum-cpu.github.io',
  
  // Since your repo is exactly svoum-cpu.github.io, 
  // your 'base' is just the root '/'.
  base: '/',
});
